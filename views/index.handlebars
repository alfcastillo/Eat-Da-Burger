<h1>
  Eat-Da-Burger!</h1>
{{!--
<img src="/public/assets/images/6-2-burger-png-image.png" alt="Burger" width="500" height="600"> --}}
<hr>
<div class="row">
  <div class="col-6">
    <h2>Burgers List!</h2>

    <ul>
      {{#each burgers}} {{#unless devoured}} {{> burger/burger-block devoured=false}} {{/unless}} {{/each}}
    </ul>
  </div>

  <div class="col-6">
    <h2>Burgers that are devoured!</h2>

    <ul>
      {{#each burgers}} {{#if devoured}} {{> burger/burger-block devoured=true}} {{/if}} {{/each}}
    </ul>
  </div>
</div>
<hr>

<div class="container" class="row">
  <h2>Add a Burger</h2>
  <form class="create-form">

    <div class="form-group">
      <label for="burger">Burger Name:</label>
      <input type="text" id="burger" name="name">
    </div>

    <button type="submit">Add Burger</button>
  </form>
</div>